Developer portrait: me.jpg (512x512, square crop of the user-supplied photo).

The About modal (src/components/landing/AboutModal.tsx) renders it in a 96px
circle. Replace this file to update the photo — the violet "S" gradient
fallback only shows while the image is missing or fails to load.
